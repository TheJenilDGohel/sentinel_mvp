# Phase 2: GPS Location Service - Plan

**Status:** Ready for execution

## Goal
Implement location permissions handling and GPS coordinate display.

## Steps
1. **Fix Permissions:** Add required location permissions to `AndroidManifest.xml` and `Info.plist` (which were missing and causing failures during MVP testing).
2. **Testing:** Add unit tests for `LocationNotifier`.
3. **Commit:** Commit Phase 2 changes.
4. **State Update:** Mark Phase 2 as complete in `ROADMAP.md` and `STATE.md`.
