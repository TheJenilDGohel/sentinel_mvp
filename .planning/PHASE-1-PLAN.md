# Phase 1: Project Scaffold & Authentication - Plan

**Status:** Ready for execution

## Goal
Verify and finalize the Flutter project setup, Firebase auth integration, and OTP-based login flow.

## Steps
1. **Dependency Setup:** Add `mocktail` for unit testing.
2. **Code Verification:** Review existing `AuthRepository` and `AuthNotifier` to ensure they meet the "static OTP" requirement.
3. **Testing:** Write unit tests for `AuthNotifier` to verify login, registration, and OTP verification logic.
4. **Commit:** Commit the verified and tested Phase 1 code.
5. **State Update:** Mark Phase 1 as complete in `ROADMAP.md` and `STATE.md`.
