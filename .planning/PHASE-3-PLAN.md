# Phase 3: SOS Emergency Alert - Plan

**Status:** Ready for execution

## Goal
Implement one-tap SOS with alert popup, Firestore persistence, and event history.

## Steps
1. **Verification:** Code handles missing location gracefully and relies on `SosRepository` for persistence.
2. **Testing:** Implement unit tests for `SosNotifier` with proper mocks.
3. **Commit:** Commit Phase 3 changes.
4. **State Update:** Mark Phase 3 as complete in `ROADMAP.md` and `STATE.md`.
